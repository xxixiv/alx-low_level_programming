#include <unistd.h>
#include <stdlib.h>
#include "main.h"
/**
*  _abs -  computes the absolute value of an integer.
* @f: the number whose absolute value is computed
* Return: returns 0
*/
int _abs(int f)
{
return (abs(f));
return (0);
}
